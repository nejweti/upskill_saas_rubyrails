<!DOCTYPE>
<html>
  <head>
  </head>
  <body>
    <p>You have recieved a message from a site  contact form , from
    <%= "#{@name}, #{@email}" %></p>
    <p> <%= "#{@body}" %>
    </p>
    </body>
</html>